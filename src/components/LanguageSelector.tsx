import * as React from 'react';

/**
 * This component now renders nothing, effectively hiding it from the page.
 * Returning null from a component is the standard way to prevent it from rendering.
 */
export function LanguageSelector() {
  return null;
}
